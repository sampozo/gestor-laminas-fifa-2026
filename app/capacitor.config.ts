import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gestor.laminas.fifa2026',
  appName: 'Gestor de Laminas FIFA 2026',
  webDir: 'dist'
};

export default config;
